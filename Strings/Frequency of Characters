#include <iostream>
#include <unordered_map>
using namespace std;

int main()
{
    string s = "programming";

    unordered_map<char, int> m;

    for(char c: s)
    {
        m[c]++;
    }

    for(auto it: m)
    {
        cout << it.first << " : " << it.second << endl;
    }
    return 0;
}
